Django>=5.0,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
psycopg2-binary>=2.9
python-decouple>=3.8
django-filter>=24.0
django-cors-headers>=4.3
drf-spectacular>=0.27
Pillow>=10.4                # ImageField backend — required for listing photos (v2)
firebase-admin>=6.5         # FCM push notifications — Milestone E.5 (apps/notifications/fcm.py)
django-simple-history>=3.7  # row-level audit trail on Listing + Market (v3.1 catalog overhaul)
django-storages[s3]>=1.14   # S3-compatible storage backend — used with Cloudflare R2 in production
celery>=5.4                 # async task queue — image resize, cache warming, daily reports
redis>=5.0                  # broker + result backend for Celery + (later) cache-aside layer
gunicorn>=22.0

# WhiteNoise — serves STATIC_ROOT directly from gunicorn without needing nginx in front. Required when
# DEBUG=False (Django's built-in /static/ handler is disabled), which is why all /static/admin/* requests
# returned 404 on Railway. Brotli adds the brotli-encoded static-file variant; gunicorn streams it when
# the browser supports it. Tiny dependency (~50KB).
whitenoise[brotli]>=6.7

# Test-only — kept here for simplicity; can be moved to a separate requirements-dev.txt later if image size matters
pytest>=8.3
pytest-django>=4.9
pytest-cov>=5.0
